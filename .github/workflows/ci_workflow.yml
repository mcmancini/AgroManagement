name: macOS & Windows Continuous Integration

on: [push, pull_request]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macOS-latest, windows-latest]
        python-version:
          - 3.11.8
    defaults:
      run:
        shell: bash -el {0}

    steps:
      - name: Use Node.js 20
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Checkout
        uses: actions/checkout@v3

      - name: Configuring Conda
        uses: conda-incubator/setup-miniconda@v2
        with:
          python-version: ${{ matrix.python-version }}
          activate-environment: anaconda-client-env
          environment-file: environment.yml
          auto-activate-base: false

      - name: Quality Assurance
        run: |
          python -m pylint --disable=R0902,R0903,R0914,E1101
          python -m black --check --show-source --statistics
          python -m isort --check --show-source --statistics

      - name: Unit Testing
        run: |
          python -m pytest ./tests/

    #   - name: Notebook Testing
    #     run: |
    #       python -m pytest --nbmake ./examples/examples.ipynb